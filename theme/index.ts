import { extendTheme } from "native-base";

export const mmtTheme = extendTheme({
  colors: {
    slateYellow: {
      200: "#E7E7E7",
    },
    slateGray: {
      50: "#f2f2f2",
    },
    darkText: {
      200: "#4A4A4A",
    },
  },
});
